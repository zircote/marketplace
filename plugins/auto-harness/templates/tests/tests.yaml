---
# {{PROJECT_NAME}} Functional Test Suite
# Generated by auto-harness
# Format: YAML test definitions
#
# Test Schema:
#   id: unique_identifier (required)
#   description: human-readable explanation (required)
#   category: grouping for filtering
#   action: instruction for Claude to execute (required)
#   expect: array of validation rules (required)
#     - contains: text that must appear
#     - not_contains: text that must NOT appear
#     - regex: pattern to match (use capture groups for save_as)
#   save_as: variable name to store captured value
#   depends_on: id of prerequisite test
#   tags: array of labels for filtering

version: "1.0"
description: "{{PROJECT_NAME}} Functional Test Suite"
generated_by: auto-harness

tests:
  # ============================================================================
  # Smoke Tests - Quick sanity checks
  # ============================================================================

  - id: smoke_basic
    description: Basic smoke test to verify test framework is working
    category: smoke
    action: >
      Confirm the test framework is operational by responding with
      'Test framework operational'
    expect:
      - contains: "operational"
      - not_contains: "error"
    tags: [smoke, critical]

# Add your tests below using the patterns shown in the examples:
#
# Example: MCP Tool Test
# - id: tool_example_basic
#   description: Basic test for example MCP tool
#   category: mcp-tools
#   action: "Call mcp__server__tool_name with param: 'value'"
#   expect:
#     - contains: "success"
#     - not_contains: "error"
#   tags: [smoke, mcp]
#
# Example: Command Test
# - id: cmd_example_basic
#   description: Basic test for example command
#   category: commands
#   action: "Run the /example command"
#   expect:
#     - contains: "expected output"
#   tags: [smoke, commands]
#
# Example: CRUD Chain (with variable capture and dependencies)
# - id: crud_create
#   description: Create a test entity
#   category: crud
#   action: "Create a new entity with name 'test'"
#   expect:
#     - contains: "created"
#     - regex: "ID:\\s+([a-f0-9]+)"
#   save_as: entity_id
#   tags: [crud]
#
# - id: crud_read
#   description: Read the created entity
#   category: crud
#   action: "Retrieve entity ${entity_id}"
#   depends_on: crud_create
#   expect:
#     - contains: "test"
#   tags: [crud]
#
# - id: crud_delete
#   description: Delete the test entity
#   category: crud
#   action: "Delete entity ${entity_id}"
#   depends_on: crud_read
#   expect:
#     - contains: "deleted"
#   tags: [crud]
